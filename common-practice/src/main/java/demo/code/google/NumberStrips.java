package demo.code.google;

import java.util.Arrays;

public class NumberStrips {

    public static void main( String[] args ) {
        int[] numbers = new int[]{
                9, 8, 7, 6, 5, 4, 5, 3, 2, 1
        };
        isStripped( numbers, 5 );
    }

    static boolean isStripped( int[] number, int stripCount ) {

        Arrays.sort( number );

        for ( int i = 0; i < number.length; i++ ) {

        }
        return true;
    }
}
